{
    "getTile": {
        "x": "$input.params('x')",
        "y": "$input.params('y')",
        "z": "$input.params('z')",
        "bucket": "$input.params('b')",
        "prefix": "$input.params('p')",
        "layerName": "$input.params('l')",
        "vizType": "$input.params('v')"
    }
}
